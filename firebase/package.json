{
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-echo --config ../firebase.json --only firestore,storage \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.2"
  },
  "private": true,
  "name": "echo-social-tests"
}
